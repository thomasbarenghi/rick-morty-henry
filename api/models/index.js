
const favoriteCharacterModels = require('./favorites');
const defaultCharacterModels = require('./defaultCharacter');
const userModels = require('./users');

module.exports = {

    favoriteCharacterModels,
    defaultCharacterModels,
    userModels,
}